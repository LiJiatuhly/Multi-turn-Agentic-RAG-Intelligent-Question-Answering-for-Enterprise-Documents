# 评测环境专用依赖（独立 venv，不要装进你的项目环境）
#
# ⚠️ 这套版本是实测跑通的组合，别随手 pip install -U：
#    ragas 0.4.3 的 llms/base.py 第 12 行硬 import
#      from langchain_community.chat_models.vertexai import ChatVertexAI
#    这个模块在 langchain-community 0.4（LangChain 1.x 线）里已经没了，
#    直接 pip install ragas 会在 import ragas 时就 ModuleNotFoundError。
#    所以 langchain 系必须钉在 0.3 线 —— 这也正是它没法和 LangGraph 1.x 共存的原因。

ragas==0.4.3

langchain==0.3.27
langchain-core==0.3.86
langchain-community==0.3.31
langchain-openai==0.3.35

# NonLLMContextRecall / NonLLMStringSimilarity 需要，缺了会在实例化指标时报 ImportError
openai==2.52.0
instructor==1.15.4
rapidfuzz==3.14.5
requests==2.34.2
pandas==3.0.5

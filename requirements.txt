# ===== 运行本项目（project/）所需依赖 =====
# 智谱大模型（聊天）走 OpenAI 兼容接口
langchain-openai>=1.0,<2

# 词嵌入直接用智谱 API（自定义轻量封装），只需要 requests，无需 torch
requests>=2.31

# 核心：LangGraph 智能体 + 向量库 + 切块
langgraph==1.2.6
langchain-qdrant==1.1.0
langchain-text-splitters==1.1.2
fastembed==0.8.0

# PDF -> Markdown（两个版本必须严格对齐，否则会报错）
pymupdf4llm==1.27.2.3
pymupdf==1.27.2.3

# 其他
gradio==6.19.0
tiktoken==0.13.0
python-dotenv==1.2.2

# 说明：
# - 已改用智谱 embedding，所以【不再需要】sentence-transformers / langchain-huggingface / torch，
#   安装体积和时间大幅减小。
# - langfuse / ragas / matplotlib / seaborn / ipykernel 这些只在教学notebook里用，运行本项目不需要。
# - Ragas 使用独立环境，版本见 evaluation/requirements-eval.txt。
